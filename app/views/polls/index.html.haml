%h1 All polls

- unless @polls.any?
  %p.bg-info.flash No polls.

%ul
  - @polls.each do |poll|
    %li
      %p
        =link_to poll.title, poll
        |
        =link_to 'Edit', edit_poll_path(poll)

%p= link_to 'New Poll', new_poll_path, class: "btn btn-default"
